# TODO
- [ ] Test end to end against sample files with different flags
- [ ] Write unit tests for `utils.py` and `subtitle.py`
- [x] `--output` flag to save subtitle files to a separate directory
- [ ] `--skip-existing` flag to skip files that already have an `.srt`
- [ ] AUR package
